{% extends "base.html.twig" %}
{% block title %} Projekt {% endblock %}
{% block body %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand disabled" href="{{ path('homepage') }}">Projekt</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('list') }}">Lista użytkowników</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('address_list') }}">Książka adresowa</a>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0" action="#">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Zaloguj</button>
            </form>
        </div>
    </nav>

    <div class="w-100 h-100">
        <div style="float: left; width: 50%; height: 100%; position: relative;">
            <a href="{{ path('list') }}"><img src="image1.jpeg" width="919px" height="1200px" />
                <div style="position: absolute; bottom: 550px; right: 165px;">
                    <p class="font-weight-bolder" style="color: whitesmoke; font-size: 64px; text-shadow: black 0.1em 0.1em 0.2em;">Lista użytkowników</p>
                </div>
            </a>
        </div>
        <div style="float: right; width: 50%; height: 100%; position: relative;">
            <a href="{{ path('address_list') }}" class="text-decoration-none"><img src="image.jpeg" width="919px" height="1200px" />
                <div style="position: absolute; bottom: 550px; right: 165px;">
                    <p class="font-weight-bolder" style="color: whitesmoke; font-size: 64px; text-shadow: black 0.1em 0.1em 0.2em;">Książka adresowa</p>
                </div>
            </a>

        </div>
    </div>


{% endblock %}