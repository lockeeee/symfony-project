{% block css %}
{% endblock %}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash-{{ label }} alert alert-warning alert-dismissible fade show" role="alert" style="width: 20%; margin-left: 40%;">
            <center>{{ message }}</center>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <script>
            $(".alert").alert('close')
        </script>
    {% endfor %}
{% endfor %}